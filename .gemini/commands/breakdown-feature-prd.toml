description = "Write product-facing feature PRDs grounded in project context or docs — for every feature in that context, or for one named feature"

prompt = """
Invoke the breakdown-feature-prd skill.
"""
